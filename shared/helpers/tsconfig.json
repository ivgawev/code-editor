{
      "extends": "@configurations/tsconfig/tsconfig.base.json",

      "compilerOptions": {
            "baseUrl": ".",
            "paths": {
                  "#/*": ["*"]
            },

            "types": ["node"]
      },

      "include": ["**/*.ts"],
      "exclude": ["node_modules"]
}
